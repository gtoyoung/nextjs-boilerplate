export type ImageProp = {
  src: string;
  description: string;
  title: string;
};

export type GoogleUser = {
  displayName: string;
  email: string;
  emailVerified: boolean;
  photoURL: string;
  uid: string;
  profileImage: string;
};
